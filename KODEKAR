#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    int n,amount=37000;
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        amount=amount-20;
        printf("%d ",amount);
    }
    printf("\n%d",amount);
    return 0;
}

